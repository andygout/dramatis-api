# Docker-served Neo4j logs display: `Bolt enabled on 0.0.0.0:7687`.
DATABASE_URL=bolt://0.0.0.0:7687
# Docker-served Neo4j is configured in `docker/docker-compose.yml` to require no authorisation: `NEO4J_AUTH=none`.
DATABASE_USERNAME=
# Docker-served Neo4j is configured in `docker/docker-compose.yml` to require no authorisation: `NEO4J_AUTH=none`.
DATABASE_PASSWORD=
NODE_ENV=e2e-test
